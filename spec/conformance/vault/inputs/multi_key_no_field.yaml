# ADR-0002 §1.2: a multi-key Vault envelope without #field projection
# raises the verbatim normative message
# "reference resolved to object; specify a sub-key with '#field'".
k: ${secret:vault:secret/dagstack/conformance/multi}
